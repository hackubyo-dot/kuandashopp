<!DOCTYPE html>
<html lang="pt-AO">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Erro Interno | KuandaShop</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <style>
        :root {
            --k-red: #E31C25;
            --k-dark: #0F0F0F;
            --k-gray: #1a1a1a;
            --code-bg: #000000;
            --code-text: #00FF9C;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--k-dark);
            background-image: 
                radial-gradient(circle at 50% 0%, #1a1a1a 0%, var(--k-dark) 80%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            overflow-x: hidden;
        }

        /* Container Principal */
        .error-wrapper {
            width: 100%;
            max-width: 900px;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* Efeito de Fundo (Glow Vermelho Sutil) */
        .glow-effect {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 600px; height: 600px;
            background: radial-gradient(circle, rgba(227, 28, 37, 0.15) 0%, transparent 70%);
            z-index: -1;
            pointer-events: none;
        }

        /* Ícone Animado */
        .icon-box {
            font-size: 5rem;
            color: var(--k-red);
            text-shadow: 0 0 30px rgba(227, 28, 37, 0.5);
            margin-bottom: 20px;
            animation: float 4s ease-in-out infinite;
        }

        .error-title {
            font-size: 4rem;
            font-weight: 800;
            line-height: 1;
            margin-bottom: 10px;
            background: linear-gradient(to bottom, #fff, #999);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .error-subtitle {
            font-size: 1.2rem;
            color: #888;
            margin-bottom: 40px;
            font-weight: 500;
        }

        /* O Terminal (Log do Erro) */
        .terminal-card {
            background: rgba(20, 20, 20, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            text-align: left;
            margin-bottom: 40px;
            transition: transform 0.3s ease;
        }
        
        .terminal-card:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .terminal-header {
            background: #111;
            padding: 12px 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .dot { width: 12px; height: 12px; border-radius: 50%; }
        .dot-red { background: #FF5F56; }
        .dot-yellow { background: #FFBD2E; }
        .dot-green { background: #27C93F; }
        
        .terminal-label {
            margin-left: auto;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.75rem;
            color: #666;
        }

        .terminal-body {
            padding: 25px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.9rem;
            line-height: 1.6;
            max-height: 300px;
            overflow-y: auto;
            color: #ddd;
        }

        .log-error { color: #FF5F56; font-weight: 700; }
        .log-trace { color: #888; font-size: 0.8rem; }
        .log-sys { color: var(--k-red); }

        /* Botões */
        .actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn-action {
            padding: 14px 30px;
            border-radius: 50px;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .btn-primary-custom {
            background: white;
            color: black;
            box-shadow: 0 5px 20px rgba(255, 255, 255, 0.1);
        }
        .btn-primary-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(255, 255, 255, 0.2);
            background: #f0f0f0;
            color: black;
        }

        .btn-outline-custom {
            background: transparent;
            color: #aaa;
            border: 1px solid rgba(255,255,255,0.2);
        }
        .btn-outline-custom:hover {
            border-color: white;
            color: white;
            background: rgba(255,255,255,0.05);
        }

        /* Scrollbar Customizada do Terminal */
        .terminal-body::-webkit-scrollbar { width: 8px; }
        .terminal-body::-webkit-scrollbar-track { background: #111; }
        .terminal-body::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }
        .terminal-body::-webkit-scrollbar-thumb:hover { background: #555; }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }
    </style>
</head>
<body>

    <div class="glow-effect"></div>

    <div class="error-wrapper text-center">
        
        <!-- Ícone -->
        <div class="icon-box">
            <i class="fas fa-server"></i>
        </div>

        <!-- Títulos -->
        <div class="error-title">Erro 500</div>
        <div class="error-subtitle">
            Algo inesperado aconteceu nos nossos servidores.<br>
            Nossa equipe técnica foi notificada automaticamente.
        </div>

        <!-- Terminal de Erro (Só aparece se houver erro) -->
        <div class="terminal-card">
            <div class="terminal-header">
                <div class="dot dot-red"></div>
                <div class="dot dot-yellow"></div>
                <div class="dot dot-green"></div>
                <div class="terminal-label">server_logs.txt — root@kuandashop</div>
            </div>
            <div class="terminal-body">
                <div style="color: #666; margin-bottom: 10px;">> Iniciando diagnóstico do sistema...</div>
                
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="mb-2">
                        <span class="log-sys">[SYSTEM ERROR]</span> 
                        <span class="log-error"><%= error.message || 'Unknown Error' %></span>
                    </div>
                    
                    <% if (process.env.NODE_ENV !== 'production' && error.stack) { %>
                        <div class="log-trace mt-3">
                            <%= error.stack %>
                        </div>
                    <% } else { %>
                        <div class="log-trace mt-2">> Stack trace oculto em produção por segurança.</div>
                    <% } %>
                <% } else { %>
                    <div class="log-error">> Erro desconhecido. Nenhum detalhe disponível.</div>
                <% } %>

                <div class="mt-3" style="color: #00FF9C">> Aguardando reinicialização... <span class="blink">_</span></div>
            </div>
        </div>

        <!-- Ações -->
        <div class="actions">
            <a href="/" class="btn-action btn-primary-custom">
                <i class="fas fa-redo-alt"></i> Tentar Novamente
            </a>
            
            <a href="javascript:history.back()" class="btn-action btn-outline-custom">
                <i class="fas fa-arrow-left"></i> Voltar
            </a>

            <a href="https://wa.me/244900000000" class="btn-action btn-outline-custom">
                <i class="fab fa-whatsapp"></i> Suporte
            </a>
        </div>

    </div>

    <!-- Script Simples para cursor piscando -->
    <script>
        setInterval(() => {
            const cursor = document.querySelector('.blink');
            if(cursor) cursor.style.opacity = cursor.style.opacity === '0' ? '1' : '0';
        }, 500);
    </script>

</body>
</html>