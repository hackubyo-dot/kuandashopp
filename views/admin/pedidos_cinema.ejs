<!DOCTYPE html>
<html lang="pt-AO">
<head>
    <style>
        /* =========================================
           KUANDA ADMIN - FINANCEIRO CINEMA
           ========================================= */
        :root {
            --k-red: #E31C25;
            --k-green: #10B981;
            --k-dark: #111827;
            --k-gray-light: #F3F4F6;
            --shadow-card: 0 10px 30px -5px rgba(0, 0, 0, 0.05);
        }

        body { background-color: var(--k-gray-light); }

        /* Header e Stats */
        .page-header {
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 30px; padding-top: 20px;
        }
        .page-title {
            font-weight: 800; font-size: 1.8rem; color: var(--k-dark);
            display: flex; align-items: center; gap: 12px;
        }
        .stat-card {
            background: white; border-radius: 16px; padding: 20px;
            box-shadow: var(--shadow-card); min-width: 200px;
            border-left: 4px solid var(--k-red);
        }
        .stat-val { font-size: 1.5rem; font-weight: 800; color: var(--k-dark); }
        .stat-label { font-size: 0.85rem; color: #6B7280; font-weight: 600; text-transform: uppercase; }

        /* Tabela Moderna */
        .requests-container {
            background: white; border-radius: 20px;
            box-shadow: var(--shadow-card); overflow: hidden;
            border: 1px solid rgba(0,0,0,0.03);
        }

        .table-custom { width: 100%; border-collapse: collapse; }
        
        .table-custom th {
            background: #F9FAFB; color: #6B7280; font-weight: 700; font-size: 0.75rem;
            text-transform: uppercase; padding: 18px 24px; text-align: left;
            border-bottom: 1px solid #E5E7EB; letter-spacing: 0.5px;
        }
        
        .table-custom td {
            padding: 20px 24px; border-bottom: 1px solid #F3F4F6;
            color: var(--k-dark); font-size: 0.95rem; vertical-align: middle;
        }
        
        .table-custom tr:last-child td { border-bottom: none; }
        .table-custom tr:hover td { background: #FAFAFA; }

        /* User Info */
        .user-cell { display: flex; align-items: center; gap: 12px; }
        .user-avatar {
            width: 40px; height: 40px; border-radius: 50%; background: #E5E7EB;
            display: flex; align-items: center; justify-content: center;
            font-weight: 700; color: #6B7280; font-size: 0.9rem;
        }
        .user-details div { line-height: 1.3; }
        .user-name { font-weight: 700; color: var(--k-dark); }
        .user-sub { font-size: 0.8rem; color: #9CA3AF; }

        /* Produto Info */
        .movie-badge {
            background: #EFF6FF; color: #3B82F6; padding: 4px 10px;
            border-radius: 6px; font-weight: 700; font-size: 0.8rem; display: inline-block;
        }
        .price-tag { font-weight: 800; color: var(--k-green); }

        /* Actions */
        .action-btn {
            border: none; padding: 8px 16px; border-radius: 8px; cursor: pointer;
            font-weight: 600; font-size: 0.85rem; transition: 0.2s;
            display: inline-flex; align-items: center; gap: 6px; text-decoration: none;
        }
        
        .btn-approve { background: #DCFCE7; color: #166534; }
        .btn-approve:hover { background: #bbf7d0; transform: translateY(-1px); }
        
        .btn-reject { background: #FEE2E2; color: #991B1B; }
        .btn-reject:hover { background: #fecaca; transform: translateY(-1px); }

        .btn-whatsapp { background: #25D366; color: white; padding: 8px; border-radius: 50%; width: 32px; height: 32px; display: flex; justify-content: center; align-items: center; }
        .btn-whatsapp:hover { opacity: 0.9; color: white; }

        /* Empty State */
        .empty-state { padding: 60px; text-align: center; color: #9CA3AF; }
        .empty-icon { font-size: 3rem; margin-bottom: 15px; opacity: 0.5; }

        @media(max-width: 768px) {
            .page-header { flex-direction: column; align-items: flex-start; gap: 20px; }
            .stat-card { width: 100%; }
            .table-responsive { overflow-x: auto; }
        }
    </style>
</head>
<body>

<div class="container pb-5">
    
    <!-- Cabeçalho -->
    <div class="page-header">
        <div>
            <h2 class="page-title">
                <i class="fas fa-file-invoice-dollar text-danger"></i>
                Gestão de Assinaturas
            </h2>
            <p class="text-muted mb-0">Confirme os pagamentos para liberar os filmes.</p>
        </div>
        
        <div class="stat-card">
            <div class="stat-val text-danger"><%= pedidos.length %></div>
            <div class="stat-label">Solicitações Pendentes</div>
        </div>
    </div>

    <!-- Lista de Pedidos -->
    <div class="requests-container">
        <div class="table-responsive">
            <table class="table-custom">
                <thead>
                    <tr>
                        <th>Cliente</th>
                        <th>Conteúdo Solicitado</th>
                        <th>Valor (Kz)</th>
                        <th>Data Pedido</th>
                        <th class="text-end">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (pedidos && pedidos.length > 0) { %>
                        <% pedidos.forEach(pedido => { %>
                            <tr>
                                <!-- Cliente -->
                                <td>
                                    <div class="user-cell">
                                        <div class="user-avatar">
                                            <%= pedido.usuario_nome.charAt(0).toUpperCase() %>
                                        </div>
                                        <div class="user-details">
                                            <div class="user-name"><%= pedido.usuario_nome %></div>
                                            <div class="user-sub"><%= pedido.email %></div>
                                            <% if (pedido.telefone) { %>
                                                <div class="user-sub mt-1">
                                                    <a href="https://wa.me/<%= pedido.telefone.replace(/\D/g,'') %>" target="_blank" class="text-success text-decoration-none" style="font-size: 0.75rem;">
                                                        <i class="fab fa-whatsapp"></i> <%= pedido.telefone %>
                                                    </a>
                                                </div>
                                            <% } %>
                                        </div>
                                    </div>
                                </td>

                                <!-- Filme -->
                                <td>
                                    <span class="movie-badge">
                                        <i class="fas fa-film me-1"></i> <%= pedido.filme_titulo %>
                                    </span>
                                </td>

                                <!-- Valor -->
                                <td>
                                    <span class="price-tag">
                                        <%= parseFloat(pedido.preco).toLocaleString('pt-AO', {style: 'currency', currency: 'AOA'}) %>
                                    </span>
                                </td>

                                <!-- Data -->
                                <td>
                                    <div class="text-dark fw-bold"><%= new Date(pedido.created_at).toLocaleDateString('pt-BR') %></div>
                                    <small class="text-muted"><%= new Date(pedido.created_at).toLocaleTimeString('pt-BR', {hour: '2-digit', minute:'2-digit'}) %></small>
                                </td>

                                <!-- Ações -->
                                <td class="text-end">
                                    <div class="d-flex justify-content-end gap-2">
                                        <!-- Form Aprovar -->
                                        <form action="/admin/pedidos-cinema/<%= pedido.id %>/aprovar" method="POST" class="d-inline">
                                            <button type="submit" class="action-btn btn-approve" title="Confirmar Pagamento">
                                                <i class="fas fa-check"></i> Liberar
                                            </button>
                                        </form>

                                        <!-- Form Rejeitar -->
                                        <form action="/admin/pedidos-cinema/<%= pedido.id %>/rejeitar" method="POST" class="d-inline" onsubmit="return confirm('Tem certeza que deseja cancelar este pedido?');">
                                            <button type="submit" class="action-btn btn-reject" title="Cancelar Pedido">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="5">
                                <div class="empty-state">
                                    <i class="fas fa-check-circle empty-icon text-success"></i>
                                    <h4>Tudo em dia!</h4>
                                    <p>Não há solicitações de acesso pendentes no momento.</p>
                                </div>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

</div>

</body>
</html>